group = 'admin'
description = ''
dependencies {
  compile project(':services')
}
jar {
  manifest {
    attributes "Main-Class": "com.admin.AdminEntryPoint"
  }
  from {
    configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
  }
}